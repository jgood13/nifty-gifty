<h1>This is a profilepage</h1>

{{#if logged_in}}
  <div class="profile-card">
    <div class="profile-card-header">
      {{!-- {{user's name??}} --}}
    </div>
    <div class="card-body">
      {{!-- {{profile??}} --}}
    </div>
  </div>
{{else}}
  <a href="./login.js">You must login first to view this page</a>
{{/if}}